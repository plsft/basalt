#!/usr/bin/env bash
# generate-baseline.sh — placeholder.
#
# Real implementation lands in TASK-0.4: runs the Python reference CLI against
# tests/parity/fixtures/{sample-vault-14,test-vault-large}/ and writes JSON
# baselines to tests/parity/baseline/. See PHASE-0.md TASK-0.4 for details.

set -euo pipefail

echo "scripts/generate-baseline.sh: placeholder — real implementation lands in TASK-0.4."
exit 0

{
  "name": "@basalt/cli",
  "version": "0.0.0",
  "description": "Basalt CLI — index, brief, audit your Markdown vault from the terminal.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "basalt": "./src/index.ts"
  },
  "scripts": {
    "build": "exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@basalt/cli: no tests yet\" && exit 0"
  }
}

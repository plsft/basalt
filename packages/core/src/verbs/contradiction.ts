// packages/core/src/verbs/contradiction.ts
// Contradiction (Cl) — SPEC.md §6. Real implementation lands in TASK-1.9.

import type { ContradictionFinding } from "./types";

export function findContradictions(): ContradictionFinding[] {
  throw new Error("findContradictions: not yet implemented (lands in TASK-1.9)");
}

// packages/core/src/verbs/buried.ts
// Buried Insight (Au) — SPEC.md §9. Real implementation lands in TASK-1.6.

import type { BuriedInsightFinding } from "./types";

export function findBuriedInsights(): BuriedInsightFinding[] {
  throw new Error("findBuriedInsights: not yet implemented (lands in TASK-1.6)");
}

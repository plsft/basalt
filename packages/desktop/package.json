{
  "name": "@basalt/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Basalt desktop app — Tauri 2 (Rust shell, system WebView) + React + Tailwind v4.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@basalt/desktop: no tests yet\" && exit 0"
  }
}

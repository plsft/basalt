{
  "name": "@basalt/web",
  "version": "0.0.0",
  "private": true,
  "description": "Basalt web cockpit — React + Tailwind v4, deployed to Cloudflare Pages.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"@basalt/web: no tests yet\" && exit 0"
  }
}
